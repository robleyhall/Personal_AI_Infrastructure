# ACTIONS / PIPELINES runtime

`bun install` once in this directory, then:

```bash
bun lib/runner.ts <action.json>
bun lib/pipeline-runner.ts <pipeline.yaml>
```

Ported from upstream v4.0.3 `.claude/PAI/ACTIONS/`. All paths rewritten.
